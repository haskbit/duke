# Duke

Duke is a modern jvm build tool made in typescript. We aim to make an easy to understand configuration system on top of the TOML specification. A similar way to manage Java/Koltin projects to the common `package.json` or `Cargo.toml` format in the javascript and rust projects.
